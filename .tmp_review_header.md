1. **High** [footer.html#L11](/K:/Coding/Site2/layouts/partials/footer.html#L11), [footer.html#L14](/K:/Coding/Site2/layouts/partials/footer.html#L14), [mode-script.html#L67](/K:/Coding/Site2/layouts/partials/mode-script.html#L67) - Shared pages render the footer `Contact` and `Merken en verdelers` links in bike mode, and the client sync only rewrites the header selectors. In drive mode, the header/menu can be in drive state while the footer still routes back to bike URLs.

2. **High** [bikeshop.md#L1](/K:/Coding/Site2/content/bikeshop.md#L1), [driveshop.md#L1](/K:/Coding/Site2/content/driveshop.md#L1), [list.html#L2](/K:/Coding/Site2/layouts/_default/list.html#L2), [header.html#L2](/K:/Coding/Site2/layouts/partials/header.html#L2) - The `/bikeshop/` and `/driveshop/` routes are not using the authored landing-page files; they resolve as section list pages instead. That bypasses the shared single-page hero/header path, drops the intended landing-page content, and the list template even hard-codes bike copy onto the drive route.

3. **Medium** [single.html#L2](/K:/Coding/Site2/layouts/_default/single.html#L2), [style.css#L235](/K:/Coding/Site2/assets/css/style.css#L235), [style.css#L912](/K:/Coding/Site2/assets/css/style.css#L912) - `banner-only` single pages still inherit the global `margin-top: -50vh` intro overlap, so the body card is pulled deep into the hero. That means the reused single-page top is not actually “image + overlay header only,” which undermines the intended home-vs-single split.

4. **Medium** [style.css#L116](/K:/Coding/Site2/assets/css/style.css#L116), [style.css#L685](/K:/Coding/Site2/assets/css/style.css#L685) - The overlay menu toggle breaks the shared nav typography by switching to `font-size: 1.5vh` and `small-caps`. On short viewports the `Menu` trigger shrinks relative to the adjacent nav links, reintroducing a header inconsistency instead of sharing the same type ramp.

5. **Low** [index.html#L14](/K:/Coding/Site2/layouts/index.html#L14), [index.html#L30](/K:/Coding/Site2/layouts/index.html#L30), [site-mode.html#L31](/K:/Coding/Site2/layouts/partials/site-mode.html#L31) - The home overview cards do not share one consistent structure across modes: bike summaries are commented out while drive summaries render, even though both modes define `summary` data. That makes the home grid change component markup when the mode changes instead of only changing content.