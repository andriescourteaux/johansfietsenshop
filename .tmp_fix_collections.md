Scoped fix is in. [layouts/_default/single.html](K:/Coding/Site2/layouts/_default/single.html) no longer contains the legacy `gallery_mode` branch, and the brand metadata in [data/collecties/bikeshop/merken-en-verdelers.toml](K:/Coding/Site2/data/collecties/bikeshop/merken-en-verdelers.toml) and [data/collecties/driveshop/merken-en-verdelers.toml](K:/Coding/Site2/data/collecties/driveshop/merken-en-verdelers.toml) now uses `/images/collecties/...` paths instead of `/images/merken-verdelers/...`. `BFK` now uses its own image entry and renders as a static card, so it no longer links to the Flanders URL. I left the media-collection partials, shared-page mode, section landing files, and `tests/verify_site.ps1` unchanged.

`hugo --destination .test-public` succeeded. `powershell -ExecutionPolicy Bypass -File tests/verify_site.ps1 -PublicDir .test-public -CssPath assets/css/style.css -HeadTemplatePath layouts/partials/head.html` now fails only on the two out-of-scope section-landing assertions for `bikeshop/index.html` and `driveshop/index.html`; the brands/gallery cleanup checks are green.