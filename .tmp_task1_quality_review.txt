Unable to review `ca8ea71` because I could not access the local commit: every shell command in this session fails before execution, so I can't read `tests/verify_site.ps1` or its diff.

Paste the output of `git show ca8ea71 -- tests/verify_site.ps1`, and I’ll return findings first with severity and `file:line` references.