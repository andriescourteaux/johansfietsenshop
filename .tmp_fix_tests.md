[tests/verify_site.ps1](K:\Coding\Site2\tests\verify_site.ps1) now checks only the four validated regressions you listed. No product files were modified.

I ran `hugo --destination .test-public`, which succeeded, then ran `powershell -ExecutionPolicy Bypass -File tests/verify_site.ps1 -PublicDir .test-public -CssPath assets/css/style.css -HeadTemplatePath layouts/partials/head.html` and left it failing in the intended red state. The failures are the expected ones:
- `mode-script` does not update shared-page footer `Contact` and `Merken` links
- the homepage hero copy has no separate bike/drive hooks, and `mode-script` does not target hero copy
- `/bikeshop/` and `/driveshop/` do not render the authored section body copy
- the legacy `gallery_mode` branch is still present, the brands data still carries legacy `/images/merken-verdelers/` paths, and BFK still renders with the Flanders URL