Updated [list.html](K:/Coding/Site2/layouts/_default/list.html) only. The section landing template now treats `/bikeshop/` and `/driveshop/` as direct-access pages by reading the authored body text from the existing `content/bikeshop.md` and `content/driveshop.md` files, rendering that copy on the section route, and replacing the hardcoded bike eyebrow with section-specific copy so `/driveshop/` no longer shows bike wording. I did not modify `tests/verify_site.ps1` or any brands/collections/gallery files.

Verification was run exactly as requested: `hugo --destination .test-public` and `powershell -ExecutionPolicy Bypass -File tests/verify_site.ps1 -PublicDir .test-public -CssPath assets/css/style.css -HeadTemplatePath layouts/partials/head.html`. Both succeeded, and the verifier output was `All site verification checks passed.`